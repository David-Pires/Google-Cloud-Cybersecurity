
---

# 🔑 Translating Classification to Access Control (IAM) | Traduzindo Classificação para Controle de Acesso (IAM)

Once data is classified, we apply the **Principle of Least Privilege (PoLP)**. Below is an example of how permissions are granted based on the data's sensitivity level within Google Cloud.
*Uma vez que os dados são classificados, aplicamos o **Princípio do Menor Privilégio (PoLP)**. Abaixo está um exemplo de como as permissões são concedidas com base no nível de sensibilidade dos dados no Google Cloud.*

---

## 📋 IAM Implementation Example | Exemplo de Implementação IAM

| Classification / Classificação | Target Resource / Recurso Alvo | Assigned Role / Papel Atribuído | Security Logic / Lógica de Segurança |
| --- | --- | --- | --- |
| **Public / Público** | Cloud Storage Bucket (Marketing) | `roles/storage.objectViewer` (to `allUsers`) | Public availability; no authentication needed for reading. |
| **Internal / Interno** | Google Drive / Shared Folder | `roles/viewer` (to `domain:company.com`) | Restricts access to authenticated corporate identities only. |
| **Confidential / Confidencial** | BigQuery Dataset (Finance) | `roles/bigquery.dataViewer` (to specific User/Group) | Access is granted to specific individuals; MFA is mandatory for these accounts. |
| **Restricted / Restrito** | Cloud KMS (Encryption Keys) | `roles/cloudkms.cryptoKeyDecrypter` | **Highly Granular:** Only the service account or senior admin can decrypt data. |

---

## 🛡️ Security Insights | Insights de Segurança

**🇺🇸 English:**

* **Service Accounts for Restricted Data:** For "Restricted" data, human access should be the exception. Use **Service Accounts** with specific roles to handle automated processing, reducing the risk of credential theft.
* **IAM Conditions:** You can add a layer of security by using **IAM Conditions**. For example: "Allow access to Confidential data only if the request comes from the corporate IP range."
* **Separation of Duties:** Ensure that the person who can *administer* the data (e.g., `storage.admin`) is not the same person who can *view* the data (e.g., `storage.objectViewer`) for sensitive levels.

**🇧🇷 Português:**

* **Contas de Serviço para Dados Restritos:** Para dados "Restritos", o acesso humano deve ser a exceção. Use **Contas de Serviço** com papéis específicos para lidar com processamento automatizado, reduzindo o risco de roubo de credenciais.
* **IAM Conditions (Condições):** Você pode adicionar uma camada de segurança usando **IAM Conditions**. Por exemplo: "Permitir acesso a dados Confidenciais apenas se a requisição vier da faixa de IP corporativa."
* **Segregação de Funções:** Garanta que a pessoa que pode *administrar* os dados (ex: `storage.admin`) não seja a mesma pessoa que pode *visualizar* os dados (ex: `storage.objectViewer`) para níveis sensíveis.

---

