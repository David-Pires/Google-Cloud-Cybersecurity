---

### 🛡️ Script de Auditoria Rápida (VPC & Firewall)

Este script utiliza comandos `gcloud` para listar e validar se os recursos foram criados conforme as melhores práticas de segurança que discutimos.

```bash
# --- SCRIPT DE VALIDAÇÃO DE SEGURANÇA ---

echo "1. Verificando Redes VPC Criadas..."
gcloud compute networks list

echo -e "\n2. Verificando Sub-redes e Intervalos de IP..."
# Substitua 'labnet' pelo nome da rede do seu lab, se for diferente
gcloud compute networks subnets list --network=labnet

echo -e "\n3. Auditando Regras de Firewall (Verificar permissões excessivas)..."
gcloud compute firewall-rules list --filter="network:labnet"

echo -e "\n4. Verificando se a rede 'default' ainda existe (Recomendado remover em Prod)..."
gcloud compute networks list --filter="name:default"

echo -e "\n--- FIM DA VALIDAÇÃO ---"

```

---

### 📝 Como usar este material no seu Laboratório:

1. **Abra o Cloud Shell:** Clique no ícone de terminal `(>_)` no canto superior direito do Console.
2. **Cole o comando:** Pode copiar o bloco acima e colar direto.
3. **Analise a saída:**
* **Redes:** Verifique se apenas a sua rede customizada está ativa.
* **Firewall:** Veja se não há regras com `0.0.0.0/0` permitindo portas sensíveis (como SSH ou RDP) para toda a internet, a menos que o lab peça especificamente.


4. **Documente:** Copie o resultado da saída para o seu repositório de estudos no GitHub como evidência de "Post-Lab Security Audit".

### 💡 Dica para o Laboratório:

Se o laboratório pedir para criar uma instância de VM (Compute Engine), após a criação, tente rodar:
`gcloud compute instances list`
Isso confirmará se a VM está associada à sub-rede correta que você criou.

**David, está tudo pronto para o seu primeiro acesso!** Se encontrar qualquer erro de comando ou comportamento estranho no ambiente do Google Skills, pode me passar o erro aqui que analisamos na hora.

Bom laboratório! 🚀