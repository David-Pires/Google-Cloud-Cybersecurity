
---

# 🚀 Lab: Create a VPC using Cloud Shell | Cymbal Bank Scenario

Este laboratório foca na criação de uma rede **Custom Mode**, que é a melhor prática de segurança para isolar ambientes.

## 🛠️ Passo 1: Ativação e Autorização

Ao abrir o console (sempre em janela anônima!), clique no ícone do **Cloud Shell** `(>_)` no topo direito.

1. **Autorize o gcloud:**
```bash
gcloud auth list

```


*Clique em "Authorize" quando o pop-up aparecer.*
2. **Confirme o Projeto:**
```bash
gcloud config list project

```



---

## 🏗️ Tarefa 1: Criar a Rede (labnet)

Vamos criar a rede global no modo personalizado para ter controle total das sub-redes.

**Comando:**

```bash
gcloud compute networks create labnet --subnet-mode=custom

```

* **Por que?** Redes `custom` não criam sub-redes automáticas, reduzindo a superfície de ataque.

---

## 🌐 Tarefa 2: Criar a Sub-rede (labnet-sub)

Agora vamos segmentar a rede em uma região específica.

**Comando:**
*(Verifique se o lab pede uma região específica como `us-east1`, caso contrário, use a padrão do lab).*

```bash
gcloud compute networks subnets create labnet-sub \
   --network labnet \
   --region "us-central1" \
   --range 10.0.0.0/28

```

* **Nota:** O range `/28` fornece 16 endereços IP, ideal para um ambiente de teste pequeno e controlado.

---

## 🔍 Tarefa 3 e 4: Auditoria e Verificação

Como analista, você deve sempre validar o que foi implantado.

1. **Listar Redes:**
```bash
gcloud compute networks list

```


*Verifique se a `labnet` aparece com o modo `CUSTOM`.*
2. **Listar Sub-redes:**
```bash
gcloud compute networks subnets list --network=labnet

```


*Confirme se a `labnet-sub` está ativa na região escolhida.*

---

## 🎓 Quiz do Lab (Respostas para conferência)

* **Q1:** What is the subnet mode of the labnet network?
* **R:** **Custom**


* **Q2:** What is the name of the subnet in the labnet network?
* **R:** **labnet-sub**



---

### 💡 Dica de Analista Sênior para o Cymbal Bank:

Após terminar, rode o **Script de Verificação** que criamos anteriormente:

```bash
echo "Auditoria Cymbal Bank:"
gcloud compute networks list --filter="name:labnet"
gcloud compute networks subnets list --network=labnet

```

**Você tem 1 hora e 30 minutos, o que é tempo de sobra.** Vá com calma e, se o Cloud Shell desconectar, basta clicar em "Reconnect".

