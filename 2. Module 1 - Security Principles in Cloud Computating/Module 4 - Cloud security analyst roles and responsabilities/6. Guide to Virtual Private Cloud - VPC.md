---

# 🌐 Virtual Private Cloud (VPC) Guide | Guia de VPC

A VPC is a private network hosted within a public cloud, providing logical isolation from other users. It forms the foundation of cloud network security through segmentation, firewalls, and VPNs.
*Uma VPC é uma rede privada hospedada dentro de uma nuvem pública, fornecendo isolamento lógico de outros usuários. Ela forma a base da segurança de rede na nuvem por meio de segmentação, firewalls e VPNs.*

---

## 📍 1. Global vs. Regional Resources | Recursos Globais vs. Regionais

É crucial para um Analista Sênior entender o escopo de cada componente para evitar falhas de arquitetura:

* **🇺🇸 VPC Network:** A **global** resource not associated with a specific region. It includes routes and firewall rules that can be accessed by any resource in the same project.
* **🇧🇷 Rede VPC:** Um recurso **global** não associado a uma região específica. Inclui rotas e regras de firewall que podem ser acessadas por qualquer recurso no mesmo projeto.
* **🇺🇸 Subnets:** **Regional** resources that divide the network into smaller, manageable IP ranges (IPv4) for improved efficiency.
* **🇧🇷 Sub-redes:** Recursos **regionais** que dividem a rede em faixas de IP (IPv4) menores e gerenciáveis para melhor eficiência.

---

## ⚙️ 2. Auto Mode vs. Custom Mode | Modo Automático vs. Personalizado

Ao criar sua rede, você deve escolher o modo de sub-rede:

| Mode / Modo | 🇺🇸 Characteristics | 🇧🇷 Características |
| --- | --- | --- |
| **Auto Mode** | Automatically creates a subnet in each region using predefined IPv4 ranges. | Cria automaticamente uma sub-rede em cada região usando faixas IPv4 predefinidas. |
| **Custom Mode** | No automatic subnets. You manually add subnets with specific IPv4/IPv6 ranges. | Sem sub-redes automáticas. Você adiciona sub-redes manualmente com faixas IPv4/IPv6 específicas. |

---

## 💻 3. Configuration via Cloud Shell | Configuração via Cloud Shell

Você pode gerenciar redes usando o comando `gcloud compute networks`. Aqui está a sintaxe prática para o seu repositório:

### **Create a Custom VPC | Criar uma VPC Personalizada**

`gcloud compute networks create example-vpc --subnet-mode=custom`

### **Create a Specific Subnet | Criar uma Sub-rede Específica**

`gcloud compute networks subnets create example-vpc-sub --network example-vpc --region us-west1 --range 10.0.0.0/28`

### **Audit and List | Auditar e Listar**

* **List all networks:** `gcloud compute networks list`
* **List subnets of a network:** `gcloud compute networks subnets list --network=example-vpc`

---

## 💡 Pro Tip: The Bridge to Automation | Dica: A Ponte para Automação

**🇺🇸 English:**
In the Google Cloud console, use the **"Equivalent command line"** link at the bottom of the creation page. It generates the exact `gcloud` commands for your selected parameters, making it easier to document or automate later.

**🇧🇷 Português:**
*No console do Google Cloud, use o link **"Equivalent command line"** na parte inferior da página de criação. Ele gera os comandos `gcloud` exatos para os parâmetros selecionados, facilitando a documentação ou automação posterior.*

---

*Reference: Google Cloud Cybersecurity Professional Certificate - Guide to Virtual Private Cloud (VPC).*


