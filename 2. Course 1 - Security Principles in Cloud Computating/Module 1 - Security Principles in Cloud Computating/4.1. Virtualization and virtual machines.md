# Virtualization & Virtual Machines | Virtualização e Máquinas Virtuais

This document explains the foundation of cloud computing: how physical hardware is transformed into flexible virtual resources.
*Este documento explica a base da computação em nuvem: como o hardware físico é transformado em recursos virtuais flexíveis.*

---

## 📍 Table of Contents | Índice
1. [English Version](#-english-version)
2. [Versão em Português](#-versão-em-português)

---

## 🇺🇸 English Version

### 🖥️ What is Virtualization?
Virtualization is the technology that creates a virtual version of physical infrastructure (servers, storage, networks). It uses **Virtual Machines (VMs)** to simulate physical computers.

### ⚙️ The Hypervisor (The Manager)
A hypervisor is the **abstraction layer** between the physical computer and the VM. It separates hardware from software and ensures VMs remain isolated from each other.

#### Types of Hypervisors:
* **Type 1 (Bare Metal):** Replaces the entire operating system of the physical computer. It is streamlined, highly secure, and most common in cloud environments.
* **Type 2 (Hosted):** Runs as an application over an existing operating system. Easier to install but has more overhead and security dependencies.

### 🚀 Advantages of Virtual Machines
1.  **Portability:** VMs are isolated entities, making them easy to move across networks (perfect for Hybrid Cloud).
2.  **Scalability:** Unlike fixed physical servers, you can add or remove CPU, memory, and storage from a VM as needed.
3.  **Testing Environments:** Developers can test code in a safe environment without affecting production, and simply delete the VM afterward.

---

## 🇧🇷 Versão em Português

### 🖥️ O que é Virtualização?
A virtualização é a tecnologia que cria uma versão virtual da infraestrutura física (servidores, armazenamento, redes). Ela utiliza **Máquinas Virtuais (VMs)** para simular computadores físicos.

### ⚙️ O Hypervisor (O Gerenciador)
O hypervisor é a **camada de abstração** entre o computador físico e a VM. Ele separa o hardware do software e garante que as VMs permaneçam isoladas umas das outras.

#### Tipos de Hypervisors:
* **Tipo 1 (Bare Metal):** Substitui todo o sistema operacional do computador físico. É mais eficiente, altamente seguro e o mais comum em ambientes de nuvem.
* **Tipo 2 (Hosted):** Roda como um aplicativo sobre um sistema operacional existente. Mais fácil de instalar, mas possui maior consumo de recursos e dependências de segurança do sistema base.

### 🚀 Vantagens das Máquinas Virtuais (VMs)
1.  **Portabilidade:** As VMs são entidades isoladas, facilitando o deslocamento entre redes (ideal para Nuvem Híbrida).
2.  **Escalabilidade:** Ao contrário de servidores físicos fixos, você pode adicionar ou remover CPU, memória e armazenamento de uma VM conforme a necessidade.
3.  **Ambientes de Teste:** Desenvolvedores podem testar códigos em um ambiente seguro sem afetar a produção e simplesmente deletar a VM depois.

---

### 💡 Key Terms | Termos Chave

| Term | Definition |
| :--- | :--- |
| **Abstraction** | Separation of hardware from software. / *Separação entre hardware e software.* |
| **VM** | Virtual Machine (Simulated computer). / *Máquina Virtual (Computador simulado).* |
| **Hypervisor** | Software that manages VMs. / *Software que gerencia as VMs.* |

---
*Foundational technology notes for the Google Cloud Cybersecurity Professional Certificate.*