

---

# ⌨️ Google Cloud Shell: The Security Analyst's CLI | O CLI do Analista de Segurança

Cloud Shell is an easily accessed Command-Line Interface (CLI) integrated into the Google Cloud Console. It allows analysts to manage resources, develop scripts, and interact with the cloud environment directly from a web browser.

### **Key Features | Principais Características**

* **Accessibility:** Accessible via web browser anywhere, anytime.
* **Pre-installed Tools:** Utilities and commands (like the **gcloud CLI**) are pre-installed for immediate use.
* **Cost & Persistence:** Provided at no cost. However, the environment is temporary and shuts down after a period of inactivity.
* **Security Note:** You cannot leave production files on operative system disks due to the shell's temporary nature.

---

## 🛠️ Practical Guide: Creating a VPC | Guia Prático: Criando uma VPC

Como Analista de Segurança, você usará o Cloud Shell para criar **Virtual Private Clouds (VPCs)**, que fornecem capacidades de rede para VMs e contêineres.

### **Step 1: Create the Network | Criar a Rede**

Use the `compute` command group to manipulate Compute Engine resources.

**Command:**
`gcloud compute networks create labnet --subnet-mode=custom`

* **`gcloud`**: Invokes the command-line tool.
* **`compute`**: Command group for Compute Engine.
* **`networks`**: Subset specifying the network resource.
* **`create`**: Action to be executed.
* **`labnet`**: The custom name of your network.

### **Step 2: Create a Custom Subnet | Criar uma Sub-rede Personalizada**

Subnets must have unique names within a given region.

**Command:**
`gcloud compute networks subnets create labnet-sub --network=labnet --range=10.10.0.0/28 --region=us-east1`

### **Step 3: Verify the Configuration | Verificar a Configuração**

Para auditar o que foi criado e garantir que a rede padrão (que pode ser deletada por segurança) não seja a única ativa:

* **List Networks:** `gcloud compute networks list`
* **List Subnets:** `gcloud compute networks subnets list --network=labnet`

---

## ✅ Analyst's Capabilities | Capacidades do Analista no Shell

Além de redes, você pode usar o Cloud Shell para:

* **Storage:** Create storage buckets and access databases.
* **Files:** View and edit configuration files.
* **Automation:** Execute **Terraform** scripts to build test, QA, or production environments.
* **Development:** Develop and deploy security applications.

---

*Reference: Google Cloud Cybersecurity Professional Certificate - Introduction to Cloud Shell.*


